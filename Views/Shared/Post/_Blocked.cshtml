@model ArchivedMediaModel
<div class="contains-ajax show center-xy">
    <p>
        <span>You have blocked this media.</span>
        <a class="ajax" asp-action="Unblock" asp-controller="Media" asp-route-id="@Model.Id" data-response-type="html" data-response-handler="replacewith">Unblock Media</a>
    </p>
</div>