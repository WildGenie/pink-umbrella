@using PinkUmbrella.ViewModels.Home
@model FeedModel
@if (Model.Items.Count > 0)
{
    foreach (var result in Model.Items) {
        <partial name="Post/_Container" model="result" />
        <hr />
    }
}
else
{
    <p>No posts to show.</p>
}