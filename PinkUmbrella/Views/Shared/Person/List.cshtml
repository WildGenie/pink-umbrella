@model List<Estuary.Actors.ActorObject>
@if (Model.Count > 0)
{
    foreach (var result in Model)
    {
        <partial name="SearchResult/PersonSearchResult" model="result" />
        <hr />
    }
}
else
{
    <p>No users to show.</p>
}