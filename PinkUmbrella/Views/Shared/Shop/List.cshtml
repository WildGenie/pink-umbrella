@model List<ShopModel>
@if (Model.Count > 0)
{
    foreach (var result in Model) {
        <partial name="Shop/DisplayResult" model="result" />
        <hr />
    }
}
else
{
    <p>No shops to show.</p>
}